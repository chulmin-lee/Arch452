<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="pack://application:,,,/UIControls;component/Styles/TextBox/TextBox/TextBoxCommon.xaml"/>
    <!--<ResourceDictionary Source="TextBoxCommon.xaml"/>-->
  </ResourceDictionary.MergedDictionaries>


  <!-- ################################################################### -->
  <!-- TextBox Normal                                                      -->
  <!-- ################################################################### -->
  <Style x:Key="CommonTextBoxStyle" TargetType="{x:Type TextBoxBase}" BasedOn="{StaticResource DefaultTextBoxStyle}">
    <Setter Property="ToolTipService.IsEnabled" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TextBoxBase}">
          <Border x:Name="Border"
                  Padding="2,0,2,0"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{StaticResource TextBox_BorderBrush.MouseOver}"
                  BorderThickness="1"
                  CornerRadius="0"
                  SnapsToDevicePixels="True">
            <ScrollViewer x:Name="PART_ContentHost"
                          Focusable="false"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="WrapTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource DefaultTextBoxStyle}">
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="AcceptsReturn" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TextBoxBase}">
          <Border x:Name="Border"
                  Padding="2,0,2,0"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{StaticResource TextBox_BorderBrush.MouseOver}"
                  BorderThickness="1"
                  CornerRadius="0"
                  SnapsToDevicePixels="True">
            <ScrollViewer x:Name="PART_ContentHost"
                          Focusable="false"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>


  <!--<Style x:Key="WrapTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource CommonTextBoxStyle}">
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="AcceptsReturn" Value="True" />
  </Style>-->

  <!-- ################################################################### -->
  <!-- TextBox Normal (Mouse)                                              -->
  <!-- ################################################################### -->
  <Style x:Key="CommonMouseTextBoxStyle" TargetType="{x:Type TextBoxBase}" BasedOn="{StaticResource DefaultTextBoxStyle}">
    <Setter Property="ToolTipService.IsEnabled" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TextBoxBase}">
          <Border x:Name="Border"
                  Padding="2,0,2,0"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource TextBox_CornerRadius}"
                  SnapsToDevicePixels="True">
            <ScrollViewer x:Name="PART_ContentHost"
                          Focusable="false"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
              <Setter Property="Foreground" Value="{StaticResource TextBox_Foreground.MouseOver}" />
              <Setter Property="Background" Value="{StaticResource TextBox_Background.MouseOver}" />
              <Setter Property="BorderBrush" Value="{StaticResource TextBox_BorderBrush.MouseOver}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="true">
              <Setter Property="Foreground" Value="{StaticResource TextBox_Foreground.Focused}" />
              <Setter Property="Background" Value="{StaticResource TextBox_Background.Focused}" />
              <Setter Property="BorderBrush" Value="{StaticResource TextBox_BorderBrush.Focused}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
              <Setter Property="Foreground" Value="{StaticResource TextBox_Foreground.Disabled}" />
              <Setter Property="Background" Value="{StaticResource TextBox_Background.Disabled}" />
              <Setter Property="BorderBrush" Value="{StaticResource TextBox_BorderBrush.Disabled}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>