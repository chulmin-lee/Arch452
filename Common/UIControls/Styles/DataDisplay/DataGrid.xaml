<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="pack://application:,,,/UIControls;component/Styles/DataDisplay/ListCommon.xaml"/>
  </ResourceDictionary.MergedDictionaries>

  <Style x:Key="Style.DataGridColumnHeader.Base" TargetType="{x:Type DataGridColumnHeader}">
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="OverridesDefaultStyle" Value="False" />
    <Setter Property="Background" Value="{StaticResource ListBox_Header_Background}" />
    <Setter Property="Foreground" Value="{StaticResource ListBox_Header_Foreground}" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="FontSize" Value="{Binding Source={StaticResource ListBox_Header_FontSize}}" />
    <Setter Property="Height" Value="{Binding Source={StaticResource ListBox_Header_Height}}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
          <Grid>
            <Border x:Name="Border" BorderThickness="0,0,0,1"
                  BorderBrush="Black"
                  Background="{TemplateBinding Background}">
            </Border>
            <ContentPresenter x:Name="HeaderContent"
                              Margin="0,0,0,1"
                              RecognizesAccessKey="True"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Border" Property="Background" Value="{StaticResource ListBox_Header_Background.MouseOver}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Style.DataGridRow.Base" TargetType="{x:Type DataGridRow}">
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="BorderBrush" Value="LightGray" />
    <Setter Property="BorderThickness" Value="0,0,0,1" />
    <Setter Property="Background" Value="{StaticResource ListBox_Background}" />

    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="DataGridRow">
          <Border x:Name="Border"
                Padding="2"
                SnapsToDevicePixels="true"
                Background="{StaticResource ListBox_Background}">
            <GridViewRowPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
          </Border>

          <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="TextElement.Foreground"  Value="{StaticResource ListBox_Foreground.Disabled}"/>
            </Trigger>

            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Border" Property="Background"  Value="{StaticResource ListBox_Background.MouseOver}"/>
              <Setter Property="BorderBrush"  Value="{StaticResource ListBox_BorderBrush.MouseOver}"/>
            </Trigger>

            <Trigger Property="IsSelected" Value="True">
              <Setter Property="Background"  Value="{StaticResource ListBox_Background.Selected}"/>
              <Setter Property="BorderBrush"  Value="{StaticResource ListBox_BorderBrush.Selected}"/>
            </Trigger>
          </ControlTemplate.Triggers>

        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="CommonDataGridStyle" TargetType="{x:Type DataGrid}">
    <Setter Property="AutoGenerateColumns" Value="False" />
    <Setter Property="Background" Value="#FF414345" />
    <Setter Property="CanUserAddRows" Value="False"/>
    <Setter Property="CanUserDeleteRows" Value="False"/>
    <Setter Property="CanUserReorderColumns" Value="True"/>
    <Setter Property="CanUserResizeRows" Value="False"/>
    <Setter Property="CanUserSortColumns" Value="True"/>
    <Setter Property="ColumnHeaderStyle" Value="{StaticResource Style.DataGridColumnHeader.Base}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="#EEEEEE" />
    <Setter Property="GridLinesVisibility" Value="All"/>
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="HorizontalGridLinesBrush" Value="#23272B" />
    <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
    <Setter Property="Margin" Value="5,10,5,5" />
    <Setter Property="RowBackground" Value="#495158" />
    <Setter Property="RowHeaderWidth" Value="0" />
    <Setter Property="RowHeight" Value="24" />
    <Setter Property="RowStyle" Value="{StaticResource Style.DataGridRow.Base}" />
    <Setter Property="SelectionMode" Value="Single" />
    <Setter Property="SelectionUnit" Value="Cell" />
    <Setter Property="VerticalAlignment" Value="Stretch"/>
    <Setter Property="VerticalGridLinesBrush" Value="#23272B" />
  </Style>

</ResourceDictionary>