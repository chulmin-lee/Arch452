<ui:CustomPopupWindow x:Class="EUMC.Server.SettingsView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:ui="clr-namespace:UIControls;assembly=UIControls"
                      WindowStartupLocation="CenterOwner"
                      xmlns:local="clr-namespace:EUMC.Server"
                      mc:Ignorable="d"
                      Title="Settings" Height="170" Width="500">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0">
      <StackPanel Margin="10" Orientation="Vertical">
        <GroupBox  Header="Log Settings">
          <Grid Margin="10,5,0,5">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="130" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Log Level" HorizontalAlignment="Left" Margin="0,-2,0,0" />

            <ComboBox Grid.Column="1" Width="150" Height="25" HorizontalAlignment="Left" Background="Yellow"
                      ItemsSource="{Binding LogLevel, Converter={ui:EnumToListConverter}, Mode=OneTime}"
                      SelectedValuePath="Value"
                      DisplayMemberPath="Description"
                      SelectedValue="{Binding LogLevel}" />
          </Grid>
        </GroupBox>
      </StackPanel>
    </Grid>

    <Grid Grid.Row="1" Margin="10,5,10,5">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="100" />
      </Grid.ColumnDefinitions>

      <Button Grid.Column="1" Content="Close" IsCancel="True"  Click="Close_Click" />
    </Grid>
  </Grid>
</ui:CustomPopupWindow>