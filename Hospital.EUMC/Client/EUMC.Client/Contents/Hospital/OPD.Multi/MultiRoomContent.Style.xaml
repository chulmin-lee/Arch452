<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:ui="clr-namespace:UIControls;assembly=UIControls"
                    xmlns:local="clr-namespace:EUMC.Client">

  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/Contents/Hospital/OPD.Multi/Common/MultiRoomVM.Style.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <DataTemplate DataType="{x:Type local:MultiRoomContentVM}">
    <Grid DataContext="{Binding ITEM}" Margin="5">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="320" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <ContentControl Grid.Column="0" Content="{Binding RoomPanel}" />

      <!-- rooms -->
      <ItemsControl Grid.Column="1" ItemsSource="{Binding Rooms}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Rows="1" Columns="{Binding RoomPerPage}" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
      </ItemsControl>
    </Grid>
  </DataTemplate>
</ResourceDictionary>