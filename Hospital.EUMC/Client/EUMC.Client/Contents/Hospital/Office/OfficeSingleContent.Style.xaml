<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:ui="clr-namespace:UIControls;assembly=UIControls"
                    xmlns:local="clr-namespace:EUMC.Client">

  <Style TargetType="{x:Type Label}">
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="FontWeight" Value="Normal" />
  </Style>

  <DataTemplate DataType="{x:Type local:OfficeSingleContentVM}">
    <Grid DataContext="{Binding ITEM}">
      <Grid>
        <!-- main-->
        <Grid>
          <Grid.RowDefinitions>
            <!-- 0.진료과 -->
            <RowDefinition Height="191" />
            <!-- 0.의사 정보 영역  -->
            <RowDefinition Height="525" />
            <!-- 1.진료중 -->
            <RowDefinition Height="230" />
            <!-- 3. 대기환자 목록 (175*4) -->
            <RowDefinition Height="1*" />

            <!--<RowDefinition Height="150" />-->
          </Grid.RowDefinitions>

          <Border Grid.Row="0" Background="{StaticResource accent2}">
            <Label Content="{Binding ROOM.DeptName}" FontWeight="ExtraBold" FontSize="100"
                   Foreground="{StaticResource primary}" HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>

          <Border Grid.Row="1" Background="#F5F5F5">
            <!-- 0.의사 정보 -->
            <Grid Margin="20,50,80,50">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="1*" />
              </Grid.ColumnDefinitions>

              <!-- 0.의사 정보 -->
              <Grid Grid.Column="0" HorizontalAlignment="Left" Margin="40,0,0,0">
                <Grid.RowDefinitions>
                  <RowDefinition Height="150" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0">
                  <Label Content="{Binding DOCTOR.DoctorName}" Style="{StaticResource CommonLabel}"
                         FontWeight="Bold"
                         FontSize="100" Foreground="#F68D38" />
                </Border>

                <Border Grid.Row="1">
                  <TextBlock Text="{Binding DOCTOR.SpecialPart}" Style="{StaticResource CommonTextBlock}"
                             FontSize="40" Foreground="{StaticResource primary}" />
                </Border>
              </Grid>

              <!-- 의사 사진 -->
              <Grid Grid.Column="2" HorizontalAlignment="Center">
                <Border Margin="12" HorizontalAlignment="Left" VerticalAlignment="Center">
                  <Image Stretch="Uniform">
                    <Image.Style>
                      <Style TargetType="{x:Type Image}">
                        <Setter Property="Source" Value="{Binding Photo}" />
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding Photo}" Value="{x:Null}">
                            <Setter Property="Source" Value="{StaticResource DEFAULT_PHOTO_LOGO}" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Image.Style>
                  </Image>
                </Border>
              </Grid>
            </Grid>
          </Border>

          <!-- 1.진료중 -->
          <Grid Grid.Row="2" Margin="0,10,0,10">
            <Grid.RowDefinitions>
              <RowDefinition Height="80" />
              <RowDefinition Height="140" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0">
              <Label Content="진료중" Style="{StaticResource CommonLabel}" FontSize="64" Foreground="#F68D38" />
            </Border>
            <Border Grid.Row="1" HorizontalAlignment="Center">
              <TextBlock FontWeight="Bold" Foreground="Black">
                <Run FontSize="120" Text="{Binding RoomPatient.PatientName}" />
                <Run FontSize="100">&#x00A0;님</Run>
              </TextBlock>
            </Border>
          </Grid>

          <!-- 3. 환자 목록 -->
          <Grid Grid.Row="3" Margin="15,0,15,20">

            <ItemsControl ItemsSource="{Binding Patients}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <UniformGrid Rows="{Binding CONFIG.ItemRows}" Columns="1" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Grid Margin="32,16,32,16">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="1*" />
                      <ColumnDefinition Width="1.8*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                      <Grid.Resources>
                        <Style TargetType="{x:Type Grid}">
                          <Setter Property="Background" Value="{StaticResource primary}" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding Index}" Value="1">
                              <Setter Property="Background" Value="{StaticResource accent2}" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </Grid.Resources>

                      <TextBlock>
                        <TextBlock.Resources>
                          <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CommonTextBlock}">
                            <Setter Property="FontSize" Value="52" />
                            <Setter Property="TextWrapping" Value="NoWrap" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="Text" Value="대기중입니다" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding Index}" Value="1">
                                <Setter Property="TextWrapping" Value="WrapWithOverflow" />
                                <Setter Property="Foreground" Value="Black" />
                                <Setter Property="Text" Value="다음순서 입니다" />
                                <Setter Property="TextAlignment" Value="Center" />
                                <Setter Property="Margin" Value="60,0,60,0" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </TextBlock.Resources>
                      </TextBlock>
                    </Grid>

                    <Grid Grid.Column="1" Background="#F2F2F2">
                      <Border HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Label Content="{Binding PatientName}"
                               FontSize="100"
                               FontWeight="Bold"
                               Foreground="{StaticResource Gray800}"
                             Style="{StaticResource CommonLabel}" />
                      </Border>
                    </Grid>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </Grid>

          <!-- 2.Seperator -->
          <!--<Grid Grid.Row="4" x:Name="logo_grid" Margin="10,25,10,5">
            <Grid.RowDefinitions>
              <RowDefinition Height="3" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Height="3" Width="{Binding ElementName=logo_grid, Path=ActualWidth}"
                  Background="{StaticResource primary}"
                  HorizontalAlignment="Center" VerticalAlignment="Center" />

            <Image Grid.Row="1" Source="{StaticResource LogoMiddle_Bottom}"
                   Height="50" Stretch="Uniform"
                   VerticalAlignment="Center" HorizontalAlignment="Center" />
          </Grid>-->
        </Grid>
      </Grid>

      <ContentControl Content="{Binding CallMessages}"
                      Visibility="{Binding CallMessages.ShowPopup, Converter={StaticResource BooleanToVisibilityConverter}}"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
  </DataTemplate>
</ResourceDictionary>