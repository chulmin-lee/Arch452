<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:ui="clr-namespace:UIControls;assembly=UIControls"
                    xmlns:local="clr-namespace:EUMC.Client">

  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/Contents/Hospital/OPD.Single/Common/SingleRoomPatient.Style.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <DataTemplate DataType="{x:Type local:OfficeSingleContentVM}">
    <Grid DataContext="{Binding ITEM}">
      <Grid.RowDefinitions>
        <!-- 0.의사 정보 영역  -->
        <RowDefinition Height="460" />
        <RowDefinition Height="10" />
        <!-- 1.진료중 -->
        <RowDefinition Height="250" />
        <!-- 3. 대기환자 목록  -->
        <RowDefinition Height="*" />
        <!-- 4. 지연사유 -->
        <RowDefinition Height="auto" />
      </Grid.RowDefinitions>

      <!-- ==================================================== -->
      <!-- 0. 의사정보                                           -->
      <!-- ==================================================== -->
      <Border Grid.Row="0" Background="#FFF5F5">
        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition Width="1*" />
          </Grid.ColumnDefinitions>

          <Grid Grid.Column="0" Margin="25,0,0,0">
            <Grid.RowDefinitions>
              <RowDefinition Height="2.2*" />
              <!-- 진료실이름-->
              <RowDefinition Height="15" />
              <!-- Seperator -->
              <RowDefinition Height="1.4*" />
              <!-- 센터 -->
              <RowDefinition Height="1.4*" />
              <!-- 과 -->
              <RowDefinition Height="4*" />
              <!-- 의사이름 -->
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding ROOM.RoomName}"
                     FontSize="95" HorizontalAlignment="Left" VerticalAlignment="Bottom" x:Name="RoomName" />

            <Border Grid.Row="1" Height="5" Width="{Binding Path=ActualWidth, ElementName=RoomName}"
                  Background="{StaticResource Window_Foreground}"
                  HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,3,0,3" />
            <TextBlock Grid.Row="2" Text="{Binding ROOM.DeptName}" FontSize="60" HorizontalAlignment="Left" Margin="0,5,0,0" />
            <TextBlock Grid.Row="3" Text="{Binding ROOM.DoctorDeptName}" FontSize="60"  HorizontalAlignment="Left" />
            <TextBlock Grid.Row="4" Text="{Binding ROOM.DoctorName}" FontSize="140" Foreground="#4C615B" HorizontalAlignment="Left"  VerticalAlignment="Center" />
          </Grid>

          <Image Grid.Column="1" Stretch="Uniform" HorizontalAlignment="Center">
            <Image.Style>
              <Style TargetType="{x:Type Image}">
                <Setter Property="Source" Value="{Binding ROOM.Photo}" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding ROOM.Photo}" Value="{x:Null}">
                    <Setter Property="Source" Value="{StaticResource DEFAULT_PHOTO_LOGO}" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Image.Style>
          </Image>
        </Grid>
      </Border>

      <!-- 1.구분선-->
      <Border Grid.Row="1" Background="#0E7758" />

      <!-- ==================================================== -->
      <!-- 2. 진료중                                             -->
      <!-- ==================================================== -->
      <Grid Grid.Row="2" Visibility="{Binding ROOM.IsRoomPatientVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Grid.RowDefinitions>
          <RowDefinition Height="2*" />
          <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="30,0,30,0" HorizontalAlignment="Center">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
          </Grid.ColumnDefinitions>
          <ui:TextBorder Grid.Column="0" Text="{Binding ROOM.RoomPatientName}" FontSize="127" HorizontalAlignment="Right" />
          <ui:TextBorder Grid.Column="1" Text="&#x00A0;님" FontSize="100" HorizontalAlignment="Left" />
        </Grid>
        <Label Grid.Row="1" Content="성함을 호명하면 들어오십시오" FontSize="47" Style="{StaticResource CommonLabelPrimary}" />
      </Grid>

      <!-- ==================================================== -->
      <!-- 3. 대기 환자 목록                                      -->
      <!-- ==================================================== -->
      <Grid Grid.Row="3" Margin="35,0,35,20">
        <ItemsControl ItemsSource="{Binding ROOM.Patients}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <UniformGrid Rows="{Binding ROOM.ItemRows}" Columns="1" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
        </ItemsControl>
      </Grid>

      <!-- ==================================================== -->
      <!-- 4. 지연 사유                                          -->
      <!-- ==================================================== -->
      <Border Grid.Row="4" Visibility="{Binding ROOM.ShowDelay, Converter={StaticResource BooleanToVisibilityConverter}}">
        <ui:TextBorder Text="{Binding ROOM.DelayMessage}" Height="90"
                       HorizontalAlignment="Right" Margin="0,0,35,0"
                       FontSize="56" FontWeight="SemiBold" />
      </Border>
    </Grid>
  </DataTemplate>
</ResourceDictionary>